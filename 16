#include <iostream>
using namespace std;

int main() {
    setlocale(0, "");  

    int vibor, razmer;
    char tekstura;

   
    cout << "ГЕОМЕТРИЧЕСКИЕ ФИГУРЫ\n";
    cout << "1. Линия\n";
    cout << "2. Квадрат\n";
    cout << "3. Прямоугольник\n";
    cout << "4. Треугольник\n";
    cout << "5. Решетка\n";
    cout << "Ваш выбор: ";
    cin >> vibor;

    if (vibor == 5) {
        system("cls");  

        cout << "ФИГУРА: РЕШЕТКА\n\n";

        cout << "Размер: ";
        cin >> razmer;

        cout << "Текстура: ";
        cin >> tekstura;

        cout << "\nРЕЗУЛЬТАТ:\n\n";

        
        for (int i = 0; i < razmer; i++) {
            for (int j = 0; j < razmer; j++) {
                if ((i + j) % 2 == 0)
                    cout << ". ";
                else
                    cout << tekstura << " ";
            }
            cout << "\n\n";
        }
    }

    return 0;
}
