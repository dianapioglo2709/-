#include <iostream>
using namespace std;

int main() {
    setlocale(0, "");

    int vibor, razmer;
    char tekstura;

    // 
    cout << "[ + ] Программа - \"Геометрические фигуры\"\n";
    cout << "[ 6 ] Крестик\n";
    cout << "[ 7 ] Плюс\n";
    cout << "[ + ] Выберите фигуру: ";
    cin >> vibor;

    if (vibor == 7) {
        system("cls");

        cout << "[ + ] Фигура: \"Плюс\"\n\n";
        cout << "[ + ] Размер: ";
        cin >> razmer;
        cout << "\n[ + ] Текстура: ";
        cin >> tekstura;

        cout << "\n[ + ] Результат:\n\n";

        
        int seredina = razmer / 2; 

        for (int i = 0; i < razmer; i++) {
            for (int j = 0; j < razmer; j++) {
                if (j == seredina || i == seredina)
                    cout << tekstura << "  ";
                else
                    cout << ".  ";
            }
            cout << "\n\n";
        }
    }

    return 0;
}
