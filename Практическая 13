#include <iostream>
using namespace std;

int main() {
    setlocale(0, "");

    int menu;
    int menu1;
    int menu2;
    int nice;
    int ram;
    char mik;
    char mik1;

    while (true) {
        cout << "Программа 'Геометрические фигуры'\n";
        cout << "[1] Линия\n";
        cout << "[2] Квадрат\n";
        cout << "[0] Выход\n";
        cout << "Выберите фигуру: ";
        cin >> menu;

        switch (menu) {
        case 0:
            cout << "Выход из программы...\n";

        case 1:
            system("cls");
            cout << "Фигура 'Линия'\n\n";
            cout << "[1] Горизонтальная\n";
            cout << "[2] Вертикальная\n\n";
            cout << "Выберите тип: ";
            cin >> menu1;

            switch (menu1) {
            case 1:
                system("cls");
                cout << "Длина линии: ";
                cin >> nice;
                cout << "Символ линии: ";
                cin >> mik;

                system("cls");
                for (int i = 0; i < nice; i++) {
                    cout << mik;
                }
                cout << endl;
                break;
            case 2:
                system("cls");
                cout << "Длина: ";
                cin >> nice;
                cout << "Символ: ";
                cin >> mik;

                system("cls");
                for (int i = 0; i < nice; i++) {
                    cout << mik << endl;
                }
                break;
            default:
                cout << "Такого пункта нет в меню\n\n";
            }
            break;

        case 2:
            system("cls");
            cout << "Фигура 'Квадрат'\n\n";
            cout << "[1] Заполненный\n";
            cout << "[2] Пустой\n\n";
            cout << "Выберите тип: ";
            cin >> menu2;

            switch (menu2) {
            case 1:
                system("cls");
                cout << "Размер: ";
                cin >> ram;
                cout << "Символ: ";
                cin >> mik1;

                if (ram <= 0) {
                    cout << "Нельзя брать минусовые размеры\n";
                    system("pause");
                    break;
                }

                system("cls");
                for (int i = 0; i < ram; i++) {
                    for (int j = 0; j < ram; j++) {
                        cout << mik1 << " ";
                    }
                    cout << endl;
                }
                break;

            case 2:
                system("cls");
                cout << "Размер: ";
                cin >> ram;
                cout << "Символ: ";
                cin >> mik1;

                if (ram <= 0) {
                    cout << "Нельзя брать минусовые размеры\n";
                    system("pause");
                    break;
                }

                system("cls");
                for (int i = 0; i < ram; i++) {
                    for (int j = 0; j < ram; j++) {
                        if (i == 0 || i == ram - 1 || j == 0 || j == ram - 1) {
                            cout << mik1 << " ";
                        }
                        else {
                            cout << "  ";
                        }
                    }
                    cout << endl;
                }
                break;

            default:
                cout << "Такого пункта нет в меню\n\n";
            }
            break;
            break;
        default:
            cout << "Такого пункта нет в меню\n\n";
        }

        cout << "\nДля продолжения нажмите Enter...";
        cin.ignore();
        cin.get();
        system("cls");
    }
    return 0;
}

