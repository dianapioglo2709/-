#include <iostream>
using namespace std;

int main() {
    setlocale(0, "");
    int number;

    cout << "[ + ] Введите число: ";
    cin >> number;

   cout << "\n[ + ] Таблица умножения" << number << ":\n" << endl;

    int i = 1;
    while (i <= 10) {
        cout << "[ + ] " << number << " × " << i << " = " << (number * i) <<endl;
        i++;
    }

    return 0;
}
